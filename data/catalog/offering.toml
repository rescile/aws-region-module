origin_resource = "portfolio"

[[create_resource]]
resource_type = "offering"
relation_type = "DEFINED_BY"
name = "compute"
[create_resource.properties]
function = "infrastructure"
display = "Compute & Virtualization",
service_id = "SVC-INF-001"
active = true
description = "Provisioning, management, and maintenance of physical and virtualized server environments located in local data centers."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
resource_type = "offering"
relation_type = "DEFINED_BY"
name = "storage"
[create_resource.properties]
function = "infrastructure"
display = "Enterprise Storage & Backup",
uid = "OFF-INF-002"
active = true
description = "Data storage allocation, block/file storage, and standardized on-premises disaster recovery and backup operations."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
resource_type = "offering"
relation_type = "DEFINED_BY"
name = "database"
[create_resource.properties]
function = "infrastructure"
display = "Database Systems",
uid = "OFF-INF-003"
active = true
description = "Managed database engines and clustered data systems hosted on-premises for application development."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
resource_type = "offering"
relation_type = "DEFINED_BY"
name = "container"
[create_resource.properties]
function = "platform"
display = "Container & Orchestration Platform",
uid = "OFF-PLT-001"
active = true
description = "On-premises Kubernetes and container runtime environments to support modern microservice workloads."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
